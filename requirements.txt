# Use PyTorch CPU wheels for just torch/torchvision (not for everything!)
--extra-index-url https://download.pytorch.org/whl/cpu

# Core dependencies
torch==2.2.2+cpu
torchvision==0.17.2+cpu
streamlit
Pillow>=10.0.0
